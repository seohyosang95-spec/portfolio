# 과제 8 제출문 초안

## 결과물 URL
- HTTPS 결과물 주소: [배포 후 입력]

## 소스 URL
- GitHub 소스 주소: [업로드 후 입력]

## 인증 구현 설명서 6항목

### ① 무엇으로 붙였나
Node.js + Express 서버와 WebAuthn 기반 `@simplewebauthn/server`, 브라우저의 `@simplewebauthn/browser`를 사용했다. 비밀번호 입력란은 만들지 않았다.

### ② 왜 그걸 골랐나
WebAuthn의 challenge 생성, 공개키 등록, 서명 검증 절차를 직접 암호 알고리즘으로 구현하는 대신 검증된 라이브러리를 사용해 과제의 핵심 흐름을 명확히 확인하기 위해 선택했다.

### ③ 어디를 어떻게 고쳤나
- 등록: `server.js` → `/api/register/options`, `/api/register/verify`
- 로그인: `server.js` → `/api/login/options`, `/api/login/verify`
- 로그아웃: `server.js` → `/api/logout`
- 비공개 자료: `server.js` → `/api/private`
- 브라우저 호출: `app.js`

### ④ 안 열리는 것을 확인한 기록
1. 로그인 없이 `/api/private` 요청 → 401
2. 다른 계정 자료 요청 → 403
3. 이미 사용한 challenge로 verify 재요청 → 400 거절
4. 패스키 두 개 중 하나 삭제 → 남은 패스키 로그인 성공 / 삭제 패스키 실패

### ⑤ AI와 나
AI는 WebAuthn 코드의 기본 구조와 테스트 흐름 작성을 도왔다. 나는 공개/비공개 범위, 가상 데이터, 계정과 패스키 이름, 실제 테스트 순서를 직접 판단했다.

### ⑥ 아직 못 막은 것
학습용 버전은 최초 계정 등록 단계에 관리자 승인/초대가 없어 실제 서비스에서는 계정 선점 위험이 있다. 실제 서비스라면 최초 등록 승인 절차를 추가해야 한다.

## 짧은 확인 방법 4줄

1. 어디로 가나요: 결과물 HTTPS 주소의 첫 화면 아래 `나만의 비공개 공간`으로 이동한다.
2. 세 단계 안에 무엇을 하나요: 계정 이름 입력 → 패스키로 로그인 → 비공개 자료 조회를 누른다.
3. 무엇이 보이면 통과인가요: 인증 완료 메시지와 가상 비공개 자료 3개, 등록된 패스키 목록이 보이면 통과다.
4. 안 될 때는 무엇이 보이나요: 미인증은 401, 다른 계정 접근은 403, 잘못되거나 재사용된 인증은 오류 메시지가 보인다.

## AI와 내 판단 3줄

- AI에게 맡긴 일: WebAuthn 등록·로그인 서버 코드와 테스트용 화면의 기본 구조 작성.
- 내가 직접 판단한 일: 공개/비공개 경계, 가상 비공개 자료, 테스트 계정과 패스키 관리 방법 결정.
- AI 말을 따르지 않은 일: 화려한 UI보다 요청·응답 증거와 인증 흐름을 우선하기로 판단했다.
